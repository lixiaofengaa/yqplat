<?xml version="1.0"?>
<odoo>
    <record id="view_form_total" model="ir.ui.view">
        <field name="name">Total Form</field>
        <field name="model">surface.total</field>
        <field name="arch" type="xml">
            <form string="Total">
                <header>
                    <!-- 此处添加按钮 -->
<!--                    <button name="button_check_isbn" type="object" string="统计车位数量" />-->
                </header>
                <sheet>
                    <group>
                        <field name="name" />
                        <field name="numstart" />
                        <field name="chargemode" />
                        <field name="sharemode" />
                        <field name="chargeformula" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_tree_total" model="ir.ui.view">
        <field name="name">Total List</field>
        <field name="model">surface.total</field>
        <field name="arch" type="xml">
<!--            <button name="action_confirm_all_sale_ordesar" string="确认" type="action" />-->
            <tree>
                <field name="name" />
                <field name="numstart" />
                <field name="chargemode" />
                <field name="sharemode" />
                <field name="chargeformula" />
            </tree>
        </field>
    </record>

</odoo>